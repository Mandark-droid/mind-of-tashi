# --- The Mind of Tashi: deployed Space runtime (Gradio SDK + ZeroGPU) --------
# The opponent runs on a dynamically-allocated local GPU via @spaces.GPU
# (transformers / PyTorch). Off-the-Grid: no cloud-API SDKs here — the model is
# local, the GPU is local; nothing phones a cloud provider at request time.
#
# The llama.cpp / GGUF runtime (Llama Champion) is the LOCAL path: clone the
# repo and `docker build` (see Dockerfile + requirements-llamacpp.txt), or
# `pip install llama-cpp-python` and set BACKEND=llamacpp. Same model, two
# runtimes — the Space picks transformers+ZeroGPU via BACKEND=transformers.
gradio[oauth]>=6.15
spaces
transformers>=5.5
torch>=2.8
accelerate
huggingface_hub>=0.27.0
# llama.cpp runtime for the self-play CHALLENGER roster (SELFPLAY_MODE=1):
# challenger GGUFs (our SFT/GRPO micro, MiniCPM5-1B, Nemotron-4B) run
# in-process on CPU — separate from the house opponent's transformers/ZeroGPU
# backend, and still Off-the-Grid (local inference, no cloud API).
# PREBUILT CPU WHEEL ONLY: compiling from the PyPI sdist OOM-kills the Space
# build container (exit 137). The abetlen index ships manylinux cp38-cp313
# wheels for ==0.3.19; if install fails anyway, the challenger roster falls
# back to the transformers/(Zero)GPU path automatically (selfplay_live).
--extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python==0.3.19
